# 讓 AI agnet 做事的通用規則

## 註解很重要

這是一個共同開發的項目，必須在代碼重要功能處寫清楚註解。

---
## 文檔與專案需高度同步

目前專案有 `docs/SDD.md` 和 `docs/py-log.md` 兩個紀錄專案狀態的文檔，必須確保這兩個文檔中的敘述時時和專案實際情況相同。
- **SDD.md**: 記錄整個專案大致架構、環境&套件依賴，和基本的專案功能。
- **py-log.md**: 詳細記錄 "後端 python" 每個模塊的功能，與相互呼叫的關係。並且對某個模塊有大幅度更動也需記錄相關變更。

---
## python 後端架構規範

有"測試相關"文檔、腳本，一律必須放在 `專案目錄/python/tests/`

---
## css 原則

**繼承和配色統一**: 若非硬性需求，或是不用不行，配色一律從 `index.css` root 中挑選，以此統一視覺效果。
**一個 component 一個 css**: 不要把所有 component 的 css 都寫在 `index.css`，會很難維護。應該落實 `index.css` 僅寫全域通用的樣式，各 component 的樣式排版要分開寫。